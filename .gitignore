# Logs
logs
*.log
npm-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets
.env
.env.local
.env.*.local

# Local sync state and personal session data — never commit
sync/.meow-ops-sync-state.json
sync/auto-sync.log
public/data/sessions.json
public/data/

# Local session-history files (Pomodoro, etc.) live in the browser, not the repo
*.history.json

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.sw?

# Deploy
.vercel

# Claude Code session metadata
.claude/
.claude-history/
